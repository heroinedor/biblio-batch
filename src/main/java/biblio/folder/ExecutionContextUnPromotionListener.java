package biblio.folder;

public class ExecutionContextUnPromotionListener {
    //TODO create an UNPromotionListener that implements a StepExecutionListener interface
    // and get the "input.file.name" parameter from the jobExecutionContext
}
