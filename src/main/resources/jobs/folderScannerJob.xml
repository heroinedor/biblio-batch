<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/batch"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch.xsd">

    <beans:bean class="org.springframework.beans.factory.annotation.RequiredAnnotationBeanPostProcessor"/>

    <!-- Job and steps import -->
    <beans:import resource="biblioBatchJob.xml"/>

    <!-- Job definition -->
    <job id="folderScannerJob" job-repository="jobRepository">
        <validator ref="paremetersValidator"/>
        <step id="fileProcess">
            <partition step="fileProcessStep" partitioner="partitioner">
                <handler grid-size="1" task-executor="taskExecutor"/>
            </partition>
        </step>
    </job>

    <beans:bean id="paremetersValidator" class="biblio.folder.FileNameParameterValidator"/>

    <beans:bean id="jobParametersExtractor"
                class="org.springframework.batch.core.step.job.DefaultJobParametersExtractor">
        <beans:property name="keys" value="fileName"/>
    </beans:bean>

    <step id="fileProcessStep">
        <job ref="biblioBatchJob" job-launcher="jobLauncher" job-parameters-extractor="jobParametersExtractor"/>
    </step>

    <beans:bean id="partitioner" class="org.springframework.batch.core.partition.support.MultiResourcePartitioner"
                scope="step">
        <beans:property name="resources" value="#{jobParameters['input.folder.name']}"/>
    </beans:bean>

    <beans:bean id="taskExecutor" class="org.springframework.core.task.SimpleAsyncTaskExecutor"/>
</beans:beans>