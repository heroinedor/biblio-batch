<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/batch"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch.xsd">

    <beans:bean class="org.springframework.beans.factory.annotation.RequiredAnnotationBeanPostProcessor"/>

    <!-- Job and steps import -->
    <beans:import resource="biblioBatchJob.xml"/>

    <!-- Job definition -->
    <job id="folderScannerJob" job-repository="jobRepository">
        <validator ref="paremetersValidator"/>
        <!-- TODO Add a step called "fileProcess" made of a partition which:
            - calls the step called "fileProcessStep"
            - has a partitioner linked to the "partitioner" bean
            - has a partition handler with a grid-size of 5 and with a task-executor linked to the "taskExecutor" bean
        -->
    </job>

    <beans:bean id="paremetersValidator" class="biblio.folder.FileNameParameterValidator"/>

    <beans:bean id="jobParametersExtractor"
                class="org.springframework.batch.core.step.job.DefaultJobParametersExtractor">
        <beans:property name="keys" value="fileName"/>
    </beans:bean>

    <step id="fileProcessStep">
        <!-- TODO Add a JobStep called "fileProcessStep" that launches the biblioBatchJob with
         the job-parameters-extractor referencing the "jobParametersExtractor" bean
        -->
    </step>

    <beans:bean id="partitioner">
        <!-- TODO link this bean to the "org.springframework.batch.core.partition.support.MultiResourcePartitioner" class
            with a property "resources" linked to the 'input.folder.name' jobParameter
        -->
    </beans:bean>

    <beans:bean id="taskExecutor" class="org.springframework.core.task.SimpleAsyncTaskExecutor"/>

</beans:beans>